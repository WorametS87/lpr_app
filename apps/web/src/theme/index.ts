import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#1e3a8a'
    },
    secondary: {
      main: '#0f766e'
    },
    background: {
      default: '#f8fafc'
    }
  }
});

export default theme;
